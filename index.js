import { get_walk } from './drunk';

export const drunk_walk = get_walk;

/*
let walk = get_walk({
  steps: 100,
  min_stepsize: 0,
  max_stepsize: 2,
  seed: 'hola',
  allow_standstill: false
});
console.log(walk);
*/
